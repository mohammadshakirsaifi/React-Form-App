import React from 'react';
import { Container } from 'react-bootstrap';

const About = () => (
  <Container className="mt-5">
    <h2>About This App</h2>
    <p>This is a sample React app using routing, lazy loading, form validation, and styling with React Bootstrap.</p>
  </Container>
);

export default About;
